#!/usr/bin/env python3
"""Interactive AI backend command center built with Textual.

This is the entry point script for the AI Backend Dashboard. The dashboard
provides a comprehensive terminal-based interface for monitoring and managing
AI backend LLM provider services including Ollama, vLLM, and llama.cpp.

Example:
    Run the dashboard interactively:

        ./ai-dashboard

    Or with Python:

        python3 scripts/ai-dashboard

Key Bindings:
    - 'r': Manual refresh
    - 'q': Quit
    - 'a': Toggle auto-refresh
    - 'ctrl+l': Clear event log

Environment Variables:
    AI_DASH_HTTP_TIMEOUT: HTTP request timeout in seconds (0.5-30, default: 3.0)
    AI_DASH_REFRESH_INTERVAL: Refresh interval in seconds (1-60, default: 5)
    AI_DASH_LOG_HEIGHT: Event log height in lines (5-50, default: 12)

For more information, see: docs/ai-dashboard.md
"""

import logging
import sys
from pathlib import Path

# Add parent directory to path to allow importing dashboard package
sys.path.insert(0, str(Path(__file__).parent))

from dashboard.app import DashboardApp

# Configure logging
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
)

if __name__ == "__main__":
    app = DashboardApp()
    app.run()
